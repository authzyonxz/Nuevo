name: Build External Injector IPA

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '15.2'

      - name: Build Unsigned App
        run: |
          echo "Compilando FreeFire External Injector..."
          mkdir -p Payload/FreeFireInjector.app
          
          # Compilar a Dylib como o binário principal do App para este exemplo de injeção externa
          xcrun -sdk iphoneos clang++ -arch arm64 -std=c++17 -fobjc-arc \
            Kernel/ExternalPatcher.mm \
            -framework Foundation -framework UIKit \
            -dynamiclib -o Payload/FreeFireInjector.app/FreeFireInjector
          
          # Criar Info.plist básico
          cat <<EOF > Payload/FreeFireInjector.app/Info.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key>
              <string>FreeFireInjector</string>
              <key>CFBundleIdentifier</key>
              <string>com.auth.FreeFireInjector</string>
              <key>CFBundleName</key>
              <string>FFInjector</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleShortVersionString</key>
              <string>1.0</string>
              <key>LSRequiresIPhoneOS</key>
              <true/>
              <key>MinimumOSVersion</key>
              <string>15.0</string>
          </dict>
          </plist>
          EOF

          # Empacotar como IPA
          zip -r FreeFireInjector.ipa Payload
          echo "Build da IPA concluído com sucesso!"
      
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: External-Injector-IPA
          path: FreeFireInjector.ipa
